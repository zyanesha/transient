{
  "name": "zyanesha",
  "version": "1.0.0",
  "private": true,
  "description": "Static HTML site styled with Tailwind CSS v4.",
  "scripts": {
    "dev": "tailwindcss -i ./src/input.css -o ./css/output.css --watch",
    "build": "tailwindcss -i ./src/input.css -o ./css/output.css --minify"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4.0.0",
    "tailwindcss": "^4.0.0"
  }
}
